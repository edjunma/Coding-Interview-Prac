function twoNumberSum(array, targetSum) {
  // Have two for loops to sum all possible pairs
  for (let i = 0; i < array.length -1; i++) {
    const firstNum = array[i];
    for (let j = i; j < array.length; j++) {
      const secondNum = array[j];
    }
  }
} 