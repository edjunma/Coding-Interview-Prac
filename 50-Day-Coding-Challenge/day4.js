// Day 4 JavaScript Coding Challenge
// Print the multiplication tables with numbers from 1 to 10

for (let i = 1; i <= 10; i++) {
  return(i);
}

function printTable(n) {
  for (let i = 1; i <= 10; i++) {
    let row = n + " * " + i + " = " + n * i;
    return row;
  }
}