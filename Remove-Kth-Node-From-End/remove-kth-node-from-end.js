// JavaScript Solution

class LinkedList {
	constructor(value) {
		this.value = value;
		this.next = next;
	}
}

// O(n) time | O(1) space

function removeKthNodeFromEnd(head, k) {
	let counter = 1;
	let first = head;
	let second = head;
}
